#pragma once

// Repo-wide version for esphome-uart-link. CalVer (YYYY.M.N), matching ESPHome
// core's scheme (e.g. 2026.8.1): N increments on each pushed change and resets
// when the month rolls over. Bump this file as part of every commit/push.
// Each component logs it in dump_config, so a running device reports its
// uart-link version at boot.
#define UART_LINK_VERSION "2026.9.0"
