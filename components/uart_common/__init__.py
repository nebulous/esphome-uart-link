# uart_common is a header-only utility library — no C++ source, no YAML schema.
# It exists so other UART transport components can share SPSCRingBuffer.
# AUTO_LOAD ensures it's copied into the build tree so #include paths resolve.
